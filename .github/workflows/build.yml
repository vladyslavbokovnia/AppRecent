name: Build Android APK
on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Build and test
        run: gradle :app:assembleRelease :app:testDebugUnitTest --stacktrace
      - name: Rename APK
        run: cp app/build/outputs/apk/release/app-release.apk app/build/outputs/apk/release/AppRecent-2.1-update.apk
      - uses: actions/upload-artifact@v4
        with:
          name: AppRecent-2.1-update
          path: app/build/outputs/apk/release/AppRecent-2.1-update.apk
